# Full mirror requirements — base runtime + AI voice pipeline.
# For a base install (no voice), use requirements-base.txt instead.
#
# Install:
#   pip install -r requirements-base.txt   # core only
#   pip install -r requirements.txt        # core + AI voice

# ── Core runtime (see requirements-base.txt) ─────────────────────────────────
PySide6
requests
psutil
caldav
icalendar
python-dotenv

# ── AI voice pipeline ────────────────────────────────────────────────────────
RealtimeSTT    # Whisper speech-to-text (pulls torch + faster-whisper)
piper-tts      # offline text-to-speech

# ── Optional: AirPlay music display ─────────────────────────────────────────
# Requires: sudo apt install libdbus-1-dev  &&  bash install_shairport.sh
# dbus-python

# ── Optional: smart home ────────────────────────────────────────────────────
# python-kasa  # Kasa / Tapo smart plugs
# phue         # Philips Hue bridge
# lifxlan      # LIFX lights
# ddgs         # DuckDuckGo web search (no API key)
